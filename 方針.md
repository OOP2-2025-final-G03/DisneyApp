## 担当
---
| 担当箇所                | メンバーID | 主な修正/追加ファイル                                                                      |
| ------------------- | ------ | ----------------------------------------------------------------------------- |
| ユーザー（User）          | k24019 | models/user.py<br>routes/user.py<br>templates/user_add.html                   |
| エリア（Area）           | k24100 | models/area.py<br>templates/area_list.html                                    |
| アトラクション（Attraction） | k24039 | models/attraction.py<br>routes/attraction.py<br>templates/attraction_add.html |
| 履歴（History）         | k24056 | models/history.py<br>routes/history.py                                        |

 
## ディレクトリの構造
 ```
  DisneyApp
  - models
    - user.py
    - area.py
    - attraction.py
    - history.py
  - routes
    - user.py
    - area.py
    - attraction.py
    - history.py
  - templates
    - user_add.html
    - user_edit.html
    - user_list.html
    - area_add.html
    - area_edit.html
    - area_list.html
    - attraction_add.html
    - attraction_edit.html
    - attraction_list.html
    - history_add.html
    - history_edit.html
    - history_list.html
  ...
 ```

## 追加/変更したいもの
---
### ユーザー
- ユーザーを追加する際、ID は自動的に割り当てられるようにする（ランダムまたは連番：0001, 0002, 0003 ...）。
- 追加時に同じ ID がすでに存在する場合は、重複しない新しい ID を再割り当てする。
- 自動採番とするため、ID の入力・編集は不要とする。
> ※ 現在はユーザー ID が重複した場合、Peewee のエラーがそのまま表示されている。

### エリア・アトラクション（共通）
- 現在 ID が存在しないため、新しく ID を追加する。
- 追加時に同じ ID がすでに存在する場合は、id + 1 などの簡易的な方法で重複しない ID を割り当てる。

### エリア
- 1つのエリアが複数のアトラクションを持てるようにする。
>実現方法（想定）
><br>Area に ID を設定する。
><br>Attraction に新しく area フィールドを作成し、所属するエリアを保持する。
><br>Area 一覧画面で各エリアに紐づくアトラクションを表示する場合は、area.attractions を使用して取得する。

### アトラクション
- Attraction の追加・編集画面に、エリア（Area）を選択するプルダウンメニューを設置する。
>※ プルダウンの表示内容には、Area モデルの name フィールドを使用する。
---

## 簡単な資料
### フィールド型一覧（SQLite）
| フィールド型 | SQLite型 | 説明 |
|-------------|----------|----------------|
| AutoField | integer | 自動的に値が増加する整数型（主キー向け） |
| BigAutoField | integer | 大きな範囲を持つ自動増分整数型 |
| IntegerField | integer | 通常の整数型 |
| BigIntegerField | integer | 64ビット相当の大きな整数を扱う |
| SmallIntegerField | integer | 小さな範囲の整数型 |
| IdentityField | 未対応 | SQLiteでは IDENTITY 列はサポートされていない |
| FloatField | real | 単精度の浮動小数点数 |
| DoubleField | real | 倍精度の浮動小数点数 |
| DecimalField | decimal | 精度を保った固定小数点数（誤差を避けたい場合に使用） |
| CharField | varchar | 可変長の文字列 |
| FixedCharField | char | 固定長の文字列 |
| TextField | text | 長い文字列（制限なし） |
| BlobField | blob | バイナリデータ（画像・ファイルなど） |
| BitField | integer | ビット列を整数として扱う |
| BigBitField | blob | 大きなビット列をバイナリとして保存 |
| UUIDField | text | UUIDを文字列として保存 |
| BinaryUUIDField | blob | UUIDをバイナリ形式で保存 |
| DateTimeField | datetime | 日付と時刻を保持 |
| DateField | date | 日付のみを保持 |
| TimeField | time | 時刻のみを保持 |
| TimestampField | integer | UNIXタイムスタンプ（秒数） |
| IPField | integer | IPアドレスを数値として保存 |
| BooleanField | integer | 真偽値（0=false, 1=true） |
| BareField | untyped | 型指定を行わない特殊なフィールド |
| ForeignKeyField | integer | 外部キー（他テーブルの主キー参照） |
